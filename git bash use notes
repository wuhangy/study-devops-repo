1、当修改了很多文件，想要commit 的文件较比较多的时候直接

     git add . ':!<文件not>' 此时就是除了文件not，其他的修改的都会添加到缓存区

2、若修改的文件较少：可直接

      git add 文件1 文件2  

3、也可以直接git add . 此时全部的文件都在缓存区在使用
      git reset HEAD

##删除远程分支
git push origin --delete 分支名
##添加到暂存区的文件如何退回
git restore --staged 文件名
##有时候生成的文件被忽略了
你在本地仓库中生成了很多文件，但 `git status` 没有检测到，通常有以下几种可能原因。请按顺序排查：

---

## 1. 文件被 `.gitignore` 忽略了

Git 会忽略 `.gitignore` 中列出的文件或目录。即使文件存在且已被修改，`git status` 也不会显示它们（除非使用 `git status --ignored`）。

**检查方法**：
```bash
# 查看是否被忽略
git status --ignored
```
如果输出中显示被忽略的文件，说明它们在 `.gitignore` 规则中。

**解决**：
- 如果这些文件应该被版本控制，请从 `.gitignore` 中移除相关规则，或使用 `git add -f` 强制添加。
- 如果确实应该忽略，那就无需处理。

---

## 2. 文件生成在仓库目录之外

你可能在仓库的上级目录或其他地方生成了文件，而不是在仓库根目录内。

**检查方法**：
```bash
# 确认当前目录是否是仓库根目录
git rev-parse --show-toplevel
```
如果生成的文件的路径不在此目录下，就不会被 Git 检测到。

**解决**：
- 确保在仓库目录内生成文件，或将文件移入仓库。

---

## 3. 文件是未跟踪的，但 `git status` 只显示已跟踪文件的修改

`git status` 默认会显示未跟踪的文件（除非你使用了 `-uno` 参数）。如果你没有看到任何未跟踪的文件，可能是：
- 这些文件确实在 `.gitignore` 中（参见第1点）。
- 这些文件所在的目录本身被忽略（如 `node_modules/`）。
- 这些文件是空文件夹（Git 不跟踪空文件夹）。

**检查方法**：
```bash
# 查看未跟踪的文件
git ls-files --others --exclude-standard
```

**解决**：
- 如果文件应该被跟踪，使用 `git add` 添加。
- 如果文件确实不需要跟踪，保持现状。

---

## 4. Git 索引或工作区状态异常

有时 Git 的索引文件损坏或工作区被某些进程锁定，导致无法检测变更。

**检查方法**：
```bash
# 刷新索引
git update-index --refresh

# 重新扫描工作区
git status
```

**解决**：
- 如果问题依旧，尝试重启终端或电脑。
- 检查是否有其他程序（如 IDE、文件同步工具）占用了文件。

---

## 5. 文件权限或所有权问题

如果你在 Windows 上使用 WSL 或 Git Bash，文件可能被设置为只读，或者 Git 进程没有读取权限。

**检查方法**：
- 查看文件属性，确认是否可读。
- 尝试手动编辑文件，看是否保存成功。

**解决**：
- 修改文件权限（`chmod` 或通过文件属性）。

---

## 6. 子模块或嵌套仓库

如果生成的目录本身是一个 Git 仓库（如子模块），则其内部变化不会显示在父仓库的 `git status` 中（除非使用 `git status --submodule`）。

**检查方法**：
```bash
# 查看是否有子模块
git submodule status
```

**解决**：
- 如果需要提交子模块内的变更，需进入子模块目录执行 `git add` 和 `git commit`。

---

## 7. Git 配置问题

检查是否启用了某些影响状态输出的配置，例如 `core.autocrlf` 或 `core.filemode` 可能导致误判。

**检查方法**：
```bash
git config --list | grep -E "core\.(autocrlf|filemode)"
```

**解决**：
- 根据实际情况调整配置，或临时关闭以测试。

---

## 总结排查步骤

1. 确认文件确实在仓库目录下：`pwd` 和 `git rev-parse --show-toplevel`。
2. 运行 `git status --ignored` 查看是否被忽略。
3. 运行 `git ls-files --others --exclude-standard` 查看未跟踪文件列表。
4. 检查 `.gitignore` 是否包含相关规则。
5. 如果以上都正常，尝试 `git update-index --refresh` 并重新运行 `git status`。

如果问题仍然存在，请提供：
- `git status` 的输出
- `.gitignore` 文件的内容
- 生成文件的路径（相对于仓库根目录）

这样我可以更精确地帮你定位问题。
